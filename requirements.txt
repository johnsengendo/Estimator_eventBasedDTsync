
#  Install:
#      pip install -r requirements.txt
#
#  Everything else the pipeline uses (os, glob, argparse, datetime, tarfile,
#  io, __future__) is in the Python standard library.
#
#  Python 3.9 or newer. Tested on 3.12.3.
# =============================================================================

# --- core numerics --------------------------------------------------------
# Arrays, the filter state updates, all metric computation.
numpy>=1.21

# --- tables ---------------------------------------------------------------
# Reading the Milan traces and the per-cell CSVs, and writing every results
# table (phase1_*_table.csv, phase1_*_sweep.csv, report_data/*.csv).
pandas>=1.5

# --- figures --------------------------------------------------------------
# All plotting. 
matplotlib>=3.5

# --- hyper-parameter search ----------------------------------------------
# Used only  for the TPE sampler that tunes the
# estimator parameters. If you skip the tuning stage and use the parameters
# already in tuned_params_real.csv, this one is not needed.
optuna>=3.0

# =============================================================================
#  Versions needed
#
#      python    3.12.3
#      numpy     2.4.4
#      pandas    3.0.2
#      matplotlib 3.10.8
#      optuna    4.9.0
#
# =============================================================================
